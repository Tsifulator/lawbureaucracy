# Hosted (search-only) deps. Local dev still uses requirements.txt + Ollama.
# onnxruntime + tokenizers replace Ollama for embedding the QUERY with bge-m3.
numpy>=2.0
onnxruntime>=1.22
tokenizers>=0.21
